host: "localhost"
port: 8080
# uncomment the next line to prevent auto connection at startup
# autoConnect: false
# uncomment the next line to use a custom token server
# tokenServerUrl: "https://example.org/token/"